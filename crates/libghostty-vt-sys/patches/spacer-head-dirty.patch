diff --git a/src/terminal/Screen.zig b/src/terminal/Screen.zig
index 41149f6da..517693e4b 100644
--- a/src/terminal/Screen.zig
+++ b/src/terminal/Screen.zig
@@ -754,6 +754,20 @@ pub fn cursorCellEndOfPrev(self: *Screen) *pagepkg.Cell {
     return page_rac.cell;
 }
 
+/// If the previous row ends with a spacer_head for a wide glyph that
+/// wrapped onto this row, convert it to a narrow cell and mark that
+/// row dirty so a pooled RenderState copies the rewrite.
+pub fn narrowPrevWrapSpacerHead(self: *Screen) void {
+    assert(self.cursor.y > 0);
+    var page_pin = self.cursor.page_pin.up(1).?;
+    page_pin.x = page_pin.node.cols() - 1;
+    const page_rac = page_pin.rowAndCell();
+    if (page_rac.cell.wide == .spacer_head) {
+        page_rac.cell.wide = .narrow;
+        page_rac.row.dirty = true;
+    }
+}
+
 /// Move the cursor right. This is a specialized function that is very fast
 /// if the caller can guarantee we have space to move right (no wrapping).
 pub fn cursorRight(self: *Screen, n: size.CellCountInt) void {
@@ -1899,6 +1913,10 @@ pub fn splitCellBoundary(
                         p_rac.row,
                         p_cells[p_row.node.cols() - 1 ..][0..1],
                     );
+                    // clearCells rewrites a different row than the one
+                    // the caller will mark dirty. Without this, a pooled
+                    // RenderState keeps the stale spacer-head pen.
+                    p_rac.row.dirty = true;
                 }
             }
         }
diff --git a/src/terminal/Terminal.zig b/src/terminal/Terminal.zig
index ffabce2fb..ff57a5d36 100644
--- a/src/terminal/Terminal.zig
+++ b/src/terminal/Terminal.zig
@@ -1548,8 +1548,7 @@ fn printCell(
                 // at the end of that row. Clear it so the previous row
                 // doesn't keep a stale spacer_head.
                 if (self.screens.active.cursor.y > 0 and self.screens.active.cursor.x <= 1) {
-                    const head_cell = self.screens.active.cursorCellEndOfPrev();
-                    if (head_cell.wide == .spacer_head) head_cell.wide = .narrow;
+                    self.screens.active.narrowPrevWrapSpacerHead();
                 }
             },
 
@@ -1573,8 +1572,7 @@ fn printCell(
                 // at the end of that row. Clear it so the previous row
                 // doesn't keep a stale spacer_head.
                 if (self.screens.active.cursor.y > 0 and self.screens.active.cursor.x <= 1) {
-                    const head_cell = self.screens.active.cursorCellEndOfPrev();
-                    if (head_cell.wide == .spacer_head) head_cell.wide = .narrow;
+                    self.screens.active.narrowPrevWrapSpacerHead();
                 }
             },
 
@@ -9544,6 +9542,40 @@ test "Terminal: eraseChars wide char wrap boundary conditions" {
     }
 }
 
+test "Terminal: eraseChars wrapped wide glyph dirties previous spacer head" {
+    const alloc = testing.allocator;
+    const io_impl = testing.io;
+    var t = try init(io_impl, alloc, .{ .rows = 3, .cols = 9 });
+    defer t.deinit(alloc);
+
+    // 9-col row: CSI 1;6H + SGR 7;31 + 界 # 界. The second wide glyph
+    // wraps, leaving a spacer_head at row 0 col 8 and the glyph at
+    // row 1 cols 0-1.
+    var s = t.vtStream();
+    defer s.deinit();
+    s.nextSlice("\x1b[1;6H\x1b[7;31m界#界");
+
+    {
+        const list_cell = t.screens.active.pages.getCell(.{ .screen = .{ .x = 8, .y = 0 } }).?;
+        try testing.expectEqual(Cell.Wide.spacer_head, list_cell.cell.wide);
+    }
+    {
+        const list_cell = t.screens.active.pages.getCell(.{ .screen = .{ .x = 0, .y = 1 } }).?;
+        try testing.expectEqual(Cell.Wide.wide, list_cell.cell.wide);
+    }
+
+    t.clearDirty();
+    s.nextSlice("\x1b[2;1H\x1b[48;5;39m\x1b[1X");
+    t.screens.active.cursor.page_pin.node.page().assertIntegrity();
+
+    try testing.expect(t.isDirty(.{ .screen = .{ .x = 0, .y = 1 } }));
+    try testing.expect(t.isDirty(.{ .screen = .{ .x = 8, .y = 0 } }));
+    {
+        const list_cell = t.screens.active.pages.getCell(.{ .screen = .{ .x = 8, .y = 0 } }).?;
+        try testing.expectEqual(Cell.Wide.narrow, list_cell.cell.wide);
+    }
+}
+
test "Terminal: reverseIndex" {
     const alloc = testing.allocator;
     const io_impl = testing.io;
